import s from "./footer.module.css"

export default function Footer() {
    return (
        <section className={s.wrapper}>
            <div className={s.thumb}>
                <b>Contact:</b>
                <a href="" className={s.link}>a a a</a>
            </div>
        </section>
    )
}